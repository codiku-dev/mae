#!/bin/sh

# Determine the directory of the script
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Run ollama-install.sh and redirect output to both log file and terminal
"$SCRIPT_DIR/ollama-install.sh" 2>&1 | tee ~/Desktop/ollama-install.log

exit 0