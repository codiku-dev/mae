#!/bin/sh

# Determine the directory of the script
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Source nvm script to ensure nvm is available
"$SCRIPT_DIR/ollama-install.sh" > ~/Desktop/ollama-install.log 2>&1 | tee /dev/tty
exit 0